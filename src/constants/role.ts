export enum Role  {
    Admin = "Admin",
    Manager = "Manager",
    Employee = "Employee"
}

export enum Status {
    Pending = "pending",
    InProgress = "InProgress",
    Completed = "Completed"
}

export enum Priority {
    Low = "Low",
    Medium = "Medium",
    High = "High"
}